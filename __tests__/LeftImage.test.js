// import React from 'react';
// import { render, screen } from '@testing-library/react';
// import LeftImageSection from '@/components/LeftImageSection';

// describe('Left Image Section ', () => {
//   test('renders the Next.js Image component', () => {
//     render(<LeftImageSection />);

//     const imageElement = screen.getByAltText('Picture of login screen');

//     // Assert that the image is in the document
//     expect(imageElement).toBeInTheDocument();hildr

//     // Assert that the image has the correct width and height
//     expect(imageElement).toHaveAttribute('width', '900');
//     expect(imageElement).toHaveAttribute('height', '900');
//   });
// });
