import { Box, styled } from '@mui/material';

export const SocialMediaIconBox = styled(Box)({
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  backgroundColor: '#F3CFC6',
  padding: '20px',
  borderRadius: '10px',
});
